all:  # This group can be renamed if needed
  hosts:
    localhost:  # You can keep this for local tasks
      ansible_connection: local
      ansible_python_interpreter: /path/to/your/python3  # Update with your actual path
    k8s_api_server:  # Define a new host for the API server
      ansible_host: 192.168.59.111  # Update with your Minikube IP address
      ansible_connection: ssh  # Connection type for the API server (might vary)


